# Launch Screenshot / GIF Shot List

1. `contextbudget plan` output with ranked files and reasons.
2. `contextbudget pack` summary line with input/saved/risk metrics.
3. `run.json` snippet showing chunk metadata (`chunk_strategy`, `chunk_reason`).
4. Strict policy failure output (`--strict --policy`).
5. `contextbudget report` Markdown summary.
6. `contextbudget diff` Markdown with context + token deltas.
7. `contextbudget benchmark` strategy comparison table.
8. GitHub Actions summary from `.github/workflows/contextbudget.yml`.
9. Python API quick demo (`BudgetGuard.pack(...)`).
10. Local telemetry JSONL file with event records.
